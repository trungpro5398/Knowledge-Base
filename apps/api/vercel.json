{
  "buildCommand": "pnpm --filter @kb/shared build && pnpm --filter api build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
