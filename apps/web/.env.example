# Supabase (from Dashboard: Project Settings → App Frameworks → .env.local)
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
# Use either name (dashboard may show PUBLISHABLE_DEFAULT_KEY):
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxx
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=xxx

# API URL - use production when deploying to Vercel
# Local: http://localhost:3001
# Production: https://knowledge-base-api.fly.dev
NEXT_PUBLIC_API_URL=https://knowledge-base-api.fly.dev

# Optional: for /api/revalidate webhook (same as API REVALIDATE_SECRET)
# REVALIDATE_SECRET=your-secret
